# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0
PortGroup           qt5 1.0
PortGroup           cmake 1.1
PortGroup           boost 1.0

github.setup        variar klogg v22.06
github.tarball_from releases
revision            0
categories          textproc
license             GPL-3
maintainers         {@eleanordoesntcode gmail.com:eleanor.publicmail} openmaintainer
description         A fast log explorer
long_description    klogg is {*}${description} based on glogg project designed with \
                    programmers and system administrators in mind and can be seen \
                    as a graphical, interactive combination of grep, less, and tail.
homepage            https://klogg.filimonov.dev/

fetch.type          git

depends_build-append \
                    port:ragel

qt5.depends_component \
                    qttools

cmake.build_type    Release

compiler.cxx_standard 2017

cmake.install_prefix \
                    ${applications_dir}

#configure.pre_args-replace \
#                    -DCMAKE_INSTALL_NAME_DIR="${cmake.install_prefix}/lib"
#configure.args-append \
#                    -DLauncher_QT_VERSION_MAJOR=5 \
#                    -DLauncher_BUILD_PLATFORM=macports
